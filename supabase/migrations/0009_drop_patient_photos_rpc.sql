-- Пациентская страница больше не показывает фото ни в каком виде — секция
-- убрана полностью. RPC-функция для анонимного чтения patient_photos по
-- токену больше не используется ни одним клиентом, оставлять её (она
-- SECURITY DEFINER и обходит RLS) — лишняя поверхность атаки.

drop function if exists public.get_patient_photos_by_token(uuid);
